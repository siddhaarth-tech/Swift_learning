let value = 10 //Immutable
var num = 0 // Mutable
num = 20
print(value)
print(num)
//value = 30 //Error because of let

let name : String
name = "Mobicip" // here the let used only for declaration so we can assign the vale once
print(name)
name = "company" //here we gets the error 